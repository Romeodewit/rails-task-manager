<h1>Your Task</h1>
<p><%= @task.title %></p>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<%= link_to "edit task", edit_task_path %>
<%= link_to "tasks", tasks_path %>
<%= link_to "Delete #{@task.title}", delete_path, method: :delete, data: {confirm: "Are you sure?" } %>